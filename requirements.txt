# ── API service ──────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0

# LangChain / LangGraph
langchain-core>=0.2.0
langchain-openai>=0.1.8
langgraph>=0.1.0

# HTTP client (used in real endpoint stubs)
httpx>=0.27.0

# Image processing — bounding-box drawing for object detection
Pillow>=10.0.0
